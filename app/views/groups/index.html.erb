<div class="container">
  <div class="row">
    <div class="col-sm-1"></div>
    <div class="col-sm-10">
      <% @groups.each do |group| %>
        <center>
          <div class="pull-left listing-box"><%=link_to group.name, group_path(group.name)%></div>
        </center>
      <% end %>
    </div>
    <div class="col-sm-1"></div>
  </div>
</div>
