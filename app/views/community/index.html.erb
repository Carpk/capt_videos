<div class="container">
  <div class="row">
    <div class="col-sm-1"></div>
    <div class="col-sm-10">
      <% @all_users.each do |user| %>
        <center><div class="pull-left listing-box"><%=link_to user.username, community_path(user.username)%></div></center>
      <% end %>
    </div>
    <div class="col-sm-1"></div>
  </div>
</div>
